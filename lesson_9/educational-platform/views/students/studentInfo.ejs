<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Info</title>
</head>
<body>
    <h1>Student Info</h1>
    <div class="student" style="border: 2px solid #000; padding: 5px;">
        <div class="student__content">
            <% if ( student.name){ %>
            <a href="/<%=student._id %>" class="student__name"><span>Ім'я ----</span><%= student.name %></a>
            <% } %>
            <% if ( student.age){ %>
            <div class="student__age"><span>Вік ----</span><%= student.age %></div>
            <% } %>

            <% if ( student.averageMark){ %>
            <div class="student__avrm"><span>середній бал ----</span><%= student.averageMark %></div>
            <% } %>
        </div>
        <div class="student__actions">
            <a href="students/register/<%=student._id %>" class="student__action button">Редагувати</a>
            <button type="submit" class="student__action button">Видалити</button>
        </div>
    </div>

    <a href="/students" class="back link">повернутись до списку</a>
</body>
</html>